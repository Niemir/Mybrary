<h2>Recently Added</h2>

<div>
    <% books.forEach(book => {%>
    <div><%= book.title%></div>
    <img src="<%= book.coverImagePath%>"
         height="150"
         width="100"
         alt=""
         style="object-fit: cover;">
    <% }) %>
</div>